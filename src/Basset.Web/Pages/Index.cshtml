@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="row">
    <div class="col-md-12">
        @if (!User.Identity.IsAuthenticated)
        {
            <a asp-action="Login" asp-controller="Account" class="btn btn-default">Log In with GitHub</a>
        }
        else
        {
            <div class="row">
                <div class="col-md-2">
                    <img src="Usernsme" alt="" class="img-thumbnail" />
                </div>
                <div class="col-md-10">
                    <h4>Username</h4>
                    <p><i class="fab fa-github"></i><a href="url">Some Login</a></p>
                </div>
            </div>
        }
    </div>
</div>
